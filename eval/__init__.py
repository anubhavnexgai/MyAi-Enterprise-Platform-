"""MyAi-Enterprise evaluation harness (Pillar 2).

Scores the correctness spine — grounding-need classification, placeholder
safety, and answer verification — against a golden set so 'gives right results'
is measurable and regressions are caught before they ship.

Run:  python -m eval.run_eval        (from the repo root)
"""
